EAX 算術計算に使われる、関数の戻り値が入れられる
EDX 算術計算に使われる

ECX 
ループの実行時に、カウント変数として使われる 前もってループの回数分を入れている
5回ループする場合は、前もって5を入れる ループ毎に1引いていき 0になるまで繰り返す

EBX 変数


ESI,EDI
文字列やデータの処理における繰り返し処理や、メモリの内容の移動に使われる
void *memcpy(void *dest, const void *source, size_t n);

ESI
スタートアドレスのインデックス(ソース)

EDI
デスティネーション


EBP

ESP
スタックの位置を指し示すためのレジスタ

Call内
[ESP+4] 第一引数
[ESP+8] 第二引数

EIP
プログラムの実行位置を指し示すためのレジスタ
通常は操作できない


セグメントレジスタ
アドレスのセグメント値を格納するための１６ビットレジスタです。
アドレスは、１６バイトを１単位とするセグメント値と、
その位置からのずれであるオフセット値という２つの値から成り立っています


CS
命令の詰まった領域の位置を示すためのレジスタ

DS 
データ領域の位置を示すためのレジスタ

SS
スタック領域の位置を示すためのレジスタ

ES
自由に使うことのできるレジスタ
FS
自由に使うことのできるレジスタ デバッグ状態を調べる
GS
自由に使うことのできるレジスタ


フラグレジスタ
eflags
加減算命令（add、sub）やcmp命令、test命令などの比較命令を実行した際に自動的にセットされます

CF（キャリーフラグ）
算術命令を処理した際に、最上位ビットに対して繰り上げまたは繰り下げが行われたときにセットされます。

ZF（ゼロフラグ）
算術命令を処理した際に、結果が0だったときにセットされます。

SF（サインフラグ）
算術命令を処理した際に、結果がマイナス値だった場合にセットされます。

OF（オーバーフラグ）
算術命令を処理した際に、結果格納用のレジスタよりも結果の値が大きいときにセットされます。
